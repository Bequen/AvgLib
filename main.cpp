
#include "lib/Debug.h"


#include <cstring>
#include <iostream>
#include <fstream>

// Only for testing purposes
int main() {
    Terminal terminal = Terminal();

    while(true) {
        terminal.draw();
    }

    return 0;
}